#include<stdio.h>
#include<conio.h>
void main()
{
int a,b,mul,s;
printf("enter number a");
scanf("%d",&a);
printf("enter number b");
scanf("%d",&b);
mul=a*b;
printf("the product of the 2 numbers is: %d",mul);
s=sqrt(mul);
if(m==a)
{
printf("the number is a perfect square");
}
else
{
printf("the number is not a perfect square");
}
getch();
}
